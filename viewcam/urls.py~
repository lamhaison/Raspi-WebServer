from django.conf.urls import patterns, url
from viewqos import views

urlpatterns = patterns('',
	url(r'^$', views.index, name='index'),
	url(r'^id=(?P<qos_id>\d+)/$', views.detail, name='detail'),
	

)

